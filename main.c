#include "push_swap.h"

int main(int ac, char **av)
{
    if (ac == 1)
        exit(1);
    if (ac == 2)
    {
        if (av[1] == NULL || )
            {
                ft_putstr_fd("Error", 2);
                exit(1);
            }
        else
            //split, check if its numebrs, atol, and put in stack a
            ;
    }
}
